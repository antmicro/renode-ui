{
  "name": "neutralino-builder",
  "version": "1.16.0",
  "scripts": {
    "build:neutralino": "neu build --release --embed-resources",
    "build:neutralino-dbg": "neu build --release --embed-resources --config-file=neutralino.dbg.config.json",
    "prepare": "[ -e bin ] || neu update"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.6.0"
  }
}
